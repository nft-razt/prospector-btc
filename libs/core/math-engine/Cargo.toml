# libs/core/math-engine/Cargo.toml
[package]
name = "prospector-core-math"
version = "0.2.0"
edition = "2021"
authors = ["Raz Podesta <metaShark Tech>"]
description = "Motor matemático de bajo nivel para operaciones de curva elíptica secp256k1 optimizado."

[dependencies]
# Curva Elíptica
secp256k1 = { version = "0.27", features = ["recovery", "rand", "global-context"] }
sha2 = "0.10"
ripemd = "0.1"
thiserror = "1.0"
hex = "0.4"
once_cell = "1.18"

# 🔥 CORRECCIÓN CRÍTICA: 'rand' movido a dependencias principales
# Necesario para SafePrivateKey::new_random()
rand = "0.8"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "secp256k1_bench"
harness = false

[lib]
name = "prospector_core_math"
path = "src/lib.rs"
