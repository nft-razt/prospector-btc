# libs/domain/mining-strategy/Cargo.toml
# =================================================================
# APARATO: STRATEGY MANIFEST (V2.1 - DEPS FIXED)
# OBJETIVO: Habilitar parsing Hexadecimal y Logging Estructurado
# =================================================================

[package]
name = "prospector-domain-strategy"
version = "0.2.0"
edition = "2021"
authors = ["Raz Podesta <metaShark Tech>"]
description = "Motores de búsqueda estratégica: Combinatoria, Diccionario, Forense y Canguro (Pollard's Lambda)."

[dependencies]
# --- SINAPSIS INTERNA (Monolito Fractal) ---
prospector-core-math = { path = "../../core/math-engine" }
prospector-core-gen = { path = "../../core/generators" }
prospector-core-probabilistic = { path = "../../core/probabilistic" }
prospector-domain-models = { path = "../models-rs" }
prospector-domain-forensics = { path = "../forensics" }

# --- PARALELISMO (MOTOR SIMD) ---
rayon = "1.8" # Permite .par_bridge() y work-stealing automático

# --- MATEMÁTICAS & CRIPTOGRAFÍA ---
num-bigint = "0.4" # Aritmética de precisión arbitraria (256 bits+)
num-traits = "0.2" # Traits numéricos (Zero, One)
sha2 = "0.10"      # Hashing para Brainwallets
hex = "0.4"        # 🔥 FIX CRÍTICO: Necesario para Kangaroo Hex Decoding

# --- UTILIDADES & OBSERVABILIDAD ---
rand = "0.8"       # Generación de números aleatorios para saltos
tracing = "0.1"    # 🔥 MEJORA: Logging estructurado (reemplaza a 'log' legacy)

[lib]
name = "prospector_domain_strategy"
path = "src/lib.rs"
